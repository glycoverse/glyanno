% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enhance-struc.R
\name{enhance_struc}
\alias{enhance_struc}
\title{Enhance glycan structure}
\usage{
enhance_struc(
  strucs,
  db = NULL,
  return_best = FALSE,
  method = "db",
  to_level = NULL
)
}
\arguments{
\item{strucs}{A \code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}} vector,
or a character vector of glycan structure strings supported by \code{\link[glyparse:auto_parse]{glyparse::auto_parse()}}.}

\item{db}{A \code{\link[glydb:glydb_structures]{glydb::glydb_structures()}} vector,
or a character vector of glycan structure strings supported by \code{\link[glyparse:auto_parse]{glyparse::auto_parse()}}.
If not provided, a default structure vector is loaded from \code{\link[glydb:glydb_structures]{glydb::glydb_structures()}}
at "intact" level.
If \code{db} has a lower or equal resolution level than \code{strucs},
the result will be the same as \code{strucs} (no enhancement).}

\item{return_best}{Logical. If \code{TRUE}, only return the best matching
structure (highest confidence) for each input structure. With
\code{method = "db"}, \code{db} must have a \code{confidence} attribute. With
\code{method = "denovo"}, branch confidence scores are used. Default is
\code{FALSE}.}

\item{method}{Enhancement method. \code{"db"} matches complete structures against
\code{db}. \code{"denovo"} reconstructs N-glycans from their core and branches.}

\item{to_level}{Target resolution level for de-novo enhancement. Required
when \code{method = "denovo"}; must be \code{"topological"} or \code{"intact"}.
Only \code{"topological"} is currently implemented.}
}
\value{
If \code{return_best=TRUE}:
An unnamed \code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}} vector with the same length as \code{strucs}.
Unmatched structures are returned as \code{NA}.
If \code{return_best=FALSE}:
A tibble with the following columns:
\itemize{
\item \code{raw}: The original glycan structures.
\item \code{enhanced}: The enhanced glycan structures.
Note that one \code{raw} glycan structure can have different \code{enhanced} glycan structures
as multiple rows in the result.
}
}
\description{
Given a glycan structure vector of any resolution level (see
\code{\link[glyrepr:get_structure_level]{glyrepr::get_structure_level()}} for details), this function gives all
possible glycan structures of higher resolution level.
}
\details{
With \code{method = "db"}, the target resolution level is determined from \code{db}.
When \code{db} is \code{NULL}, the default \code{\link[glydb:glydb_structures]{glydb::glydb_structures()}} at "intact"
level is used. With \code{method = "denovo"}, N-glycans are reconstructed from
their core and branches without requiring a structure database.

Topological de-novo enhancement preserves optional core fucose and
bisecting GlcNAc residues. Complex N-glycan branches are matched against the
internal branch data. For hybrid N-glycans, the all-Hex arm is assigned as
mannose and the HexNAc-bearing arm uses branch matching. High-mannose
candidates are constrained to core-aligned subtrees of the Glc3Man9
precursor.
}
\examples{
# From topological level to intact level
db_intact <- c("Gal(b1-3)GalNAc(a1-", "Gal(b1-4)GalNAc(a1-")
enhance_struc("Gal(??-?)GalNAc(??-", db = db_intact)

# From basic level to topological level
db_topo <- "Gal(??-?)GalNAc(??-"
enhance_struc("Hex(??-?)HexNAc(??-", db = db_topo)

# From partial level to intact level
enhance_struc("Gal(b1-?)GalNAc(a1-", db = db_intact)

# De-novo enhancement of an N-glycan
n_basic <- glyrepr::n_glycan_core(linkage = FALSE, mono_type = "generic")
enhance_struc(
  n_basic,
  method = "denovo",
  to_level = "topological",
  return_best = TRUE
)

}
