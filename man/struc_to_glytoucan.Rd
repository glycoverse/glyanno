% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/struc-to-glytoucan.R
\name{struc_to_glytoucan}
\alias{struc_to_glytoucan}
\title{Assign GlyTouCan accessions to glycan structures}
\usage{
struc_to_glytoucan(strucs)
}
\arguments{
\item{strucs}{A \code{\link[glyrepr:glycan_structure]{glyrepr::glycan_structure()}} vector,
or a character vector of glycan text representations supported by \code{\link[glyparse:auto_parse]{glyparse::auto_parse()}}.
The glycan structure must have "concrete" monosaccharides (e.g., Gal, GalNAc).
Inputs with unresolved floating parts or substituents produce a warning and
return \code{NA}.}
}
\value{
A character vector of GlyTouCan accessions corresponding to the input glycan structures.
If a structure cannot be converted to a GlyTouCan accession, the corresponding entry will be \code{NA}.
}
\description{
This function takes a vector of glycan structures and returns the
corresponding GlyTouCan accessions. It first looks up structures from
\link[glydb:glydb_data]{glydb::glydb_data}, then uses the GlycanFormatConverter API maintained by
the Glycosmos project for structures not found locally.
}
\details{
For "topological" structures (e.g., "Gal(??-?)GalNAc(??-"),
this function will first call \code{\link[glyrepr:fill_anomer_pos]{glyrepr::fill_anomer_pos()}} to fill in the missing
anomeric positions before looking up accessions. This is necessary because
all glycan structures in GlyTouCan must have defined anomeric positions.
}
